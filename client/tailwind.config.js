/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // src 폴더의 모든 JS, JSX, TS, TSX 파일에 대해 Tailwind CSS가 작동하도록 설정
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
